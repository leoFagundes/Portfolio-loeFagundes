import Title from '../Title';
import style from './Certificates.module.scss';

export default function Certificates() {
    return (
        <section id='Certificates' className={style.Certificates}>
            <Title titleName='Certificados'/>
        </section>
    )
}